# Copy to .env.local and adjust per environment.

# Base URL used for metadata (canonical links, Open Graph, sitemap).
# Update to the production domain before deploying.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
